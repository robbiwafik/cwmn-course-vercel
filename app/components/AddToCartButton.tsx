'use client'

import React from 'react'

const AddToCartButton = () => {
  return <button className='btn ' onClick={() => console.log('Clicked')}>Add to Cart</button>
}

export default AddToCartButton